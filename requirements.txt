# Core investment agent (investment_agent.py + app.py)
anthropic>=0.40.0
flask>=3.0.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
tabulate>=0.9.0
colorama>=0.4.6

# AI Hedge Fund (hedge_fund_web.py) — installed by setup_hedge_fund.sh
langchain>=0.3.7
langgraph>=0.2.56
langchain-anthropic>=0.3.5
langchain-openai>=0.3.5
langchain-core>=0.3.0
rich>=13.9.4
questionary>=2.1.0
python-dateutil>=2.8.0

# Note: multitasking (yfinance dep) may need manual install if pip build fails:
# pip download multitasking --no-deps && tar xzf multitasking*.tar.gz && python setup.py install
